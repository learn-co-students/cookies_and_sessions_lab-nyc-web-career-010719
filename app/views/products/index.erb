<% cart.each do |item| %>
<%= item %>
<% end %>

<%= form_tag({controller: 'products', action: 'add_to_cart', method: 'post'}) do %>
  <input name="product" >
  <input type="submit" value="add to cart">
<% end %>
